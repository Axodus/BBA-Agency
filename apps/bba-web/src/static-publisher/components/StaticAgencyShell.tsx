import { Drawer, Button, SkipLink } from "@bba/ui";
import { NavLink, Outlet } from "react-router-dom";
import { translate } from "../../i18n/index.js";
const links = [{ to: "/", label: translate("navigation.home") }, { to: "/services/publisher", label: translate("navigation.publisher") }, { to: "/projects", label: translate("navigation.projects") }, { to: "/settings/ai", label: translate("navigation.aiModels") }] as const;
function Navigation() { return <nav aria-label="Primary navigation">{links.map((link) => <NavLink end={link.to === "/"} key={link.to} to={link.to}>{link.label}</NavLink>)}</nav>; }
export function StaticAgencyShell() { return <div className="agency-static"><SkipLink /><header className="agency-header"><NavLink className="agency-wordmark" to="/">BBA Agency</NavLink><div className="agency-desktop-nav"><Navigation /></div><div className="agency-header-actions"><NavLink className="agency-start" data-action="create-project" to="/services/publisher/new">{translate("actions.createProject")}</NavLink><div className="agency-mobile-nav"><Drawer title="Navigation" description="Services and Projects" trigger={<Button variant="secondary">{translate("actions.menu")}</Button>}><Navigation /><NavLink to="/platform-diagnostics">{translate("navigation.platformDiagnostics")}</NavLink></Drawer></div></div></header><main id="main-content"><Outlet /></main><footer className="agency-footer"><strong>BBA Agency</strong><p>Communication, research, and institutional production coordinated by AI and accountable to people.</p><NavLink to="/platform-diagnostics">Technical Platform diagnostics</NavLink></footer></div>; }
