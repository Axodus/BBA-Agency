# Mission Infrastructure

## Public API do módulo

The infrastructure barrel exports only `InMemoryMissionRepository`, a
deterministic reference adapter for development and contract tests. It is not a
production persistence claim.
