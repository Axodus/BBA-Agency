# Mission Ports

## Public API do módulo

The ports barrel exports the `MissionRepository` interface. The port belongs to
the Mission consumer and exposes optimistic save, Tenant-scoped lookup, and
existence checks.
