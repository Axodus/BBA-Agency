# Errors

## Public API do módulo

Exports are available from `../errors/index.ts`: the domain error hierarchy,
including validation, invariant, Tenant, and optimistic-concurrency failures,
and their stable error codes.
