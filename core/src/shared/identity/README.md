# Identity

## Public API do módulo

Exports are available from `../identity/index.ts`: the opaque `Identity` base,
the deterministic `IdentityFactory`, and all canonical BBA ID types, including
Authority and Approval identities introduced for Human Governance.
