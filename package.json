{
  "name": "bba-agency",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.1.2",
  "engines": {
    "node": ">=24.0.0"
  },
  "description": "BBA Agency application workspace",
  "scripts": {
    "contracts:check": "node contracts/openapi/check-contract.mjs && node contracts/agency/check-contract.mjs",
    "agency:contracts:check": "node contracts/agency/check-contract.mjs",
    "agency:check-language": "node tools/check-agency-language.mjs",
    "agency:check": "pnpm agency:check-language && pnpm --filter @bba/publisher-prototype check && pnpm --filter @bba/agency-runtime-http check",
    "workspace:check": "node tools/check-active-workspace.mjs",
    "web:dev": "pnpm --filter @bba/web dev",
    "web:build": "pnpm --filter @bba/web build",
    "web:check": "pnpm agency:check-language && node tools/check-frontend-boundaries.mjs && pnpm --dir clients/typescript build && pnpm --filter @bba/publisher-prototype build && pnpm --filter @bba/ui check && pnpm --filter @bba/app-shell check && pnpm --filter @bba/sdk-react check && pnpm --filter @bba/web check",
    "api:dev": "pnpm --filter @bba/api dev",
    "api:infra:up": "docker compose -f docker-compose.api.yml up -d --wait",
    "api:infra:down": "docker compose -f docker-compose.api.yml down",
    "api:build": "pnpm --filter @bba/api build",
    "api:check": "pnpm --filter @bba/api check",
    "api:start": "pnpm --filter @bba/api start",
    "dev": "pnpm web:dev",
    "build": "pnpm api:build",
    "start": "node apps/api/dist/main.js",
    "check": "pnpm workspace:check && pnpm contracts:check && pnpm api:check && pnpm --filter @bba/platform-core check && pnpm web:build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Axodus/BBA-Agency.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/Axodus/BBA-Agency/issues"
  },
  "homepage": "https://github.com/Axodus/BBA-Agency#readme"
}
